# Backend API URL
VITE_API_URL=http://127.0.0.1:8000

# Stripe publishable key (starts with pk_test_ for test, pk_live_ for production)
# Leave empty to use demo mode (no real charges, no card input shown)
VITE_STRIPE_PUBLISHABLE_KEY=
